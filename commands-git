/// Pour cloner un repository

La commande : git clone + url ( disponible dans notre repository ) 


/// Les commandes de push 

-git add + fichier 
-git commit -m "un commentaire"
-git push origin master 

// all commands 


Ajout du remote > local  avec la commande git pull 

//commande pour voir dans quel branch nous somme
         - git branch
switcher de branch : 
            - git checkout + nom de la branch

